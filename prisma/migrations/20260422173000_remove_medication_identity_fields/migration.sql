-- Restored missing migration file to satisfy Prisma migration history.
-- This migration is intentionally a no-op because the related schema changes
-- are already represented by current schema state/migrations.
SELECT 1;
